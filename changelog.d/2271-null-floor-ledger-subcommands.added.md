- **`dual-build-ledger.sh` gains a `null-floor-record` / `null-floor-status` pair for the pairwise judge's A/A noise floor** (temperloop#2271). Two dual-build instrument checks that ran the SAME model in both arms still got confident, order-stable preferences from the judge (margins 17.5 and 17), so the harness needs the judge's noise floor on record before any margin can mean anything. `null-floor-record` writes `null-floor.json` — `n`, `judge_model`, `arm_model`, `margins[]`, `order_agreement_rate`, `arm_failure_rate`, `recorded_at` — atomically beside `rows.jsonl` and `calibration.json` under the same `--dir` / `DUAL_BUILD_LEDGER_DIR` ledger root (no path flag of its own), refusing any malformed input without writing. `null-floor-status` prints it back as one JSON line with `outcome: NULL_FLOOR`, or `outcome: NULL_FLOOR_UNAVAILABLE` plus a reason and a non-zero exit when the file is absent, truncated, missing a field, or carries a `schema_version` the reader does not know — never an empty success. Nothing reads the file yet: every existing subcommand and `rows.jsonl` are unchanged, and `claude/presentation-plane.md` lists the file's shape as a frozen surface.
